# Site components

`HomeView.tsx` composes named landing-page sections from localized data kept in
`app/page.tsx`. `LegalPage.tsx` owns the shared legal-document shell and
`Locale.tsx` owns locale selection. Components contain no release plans or
internal roadmap content.

Rendered-route assertions live in `tests/rendered-html.test.mjs`.
